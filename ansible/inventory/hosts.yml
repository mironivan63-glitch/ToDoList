all:
  hosts:
    scadtech:
      ansible_host: scadtech  # твой домен
      ansible_user: scadtech  # замени на твоего пользователя на VM
      ansible_ssh_private_key_file: ~/.ssh/id_rsa  # путь к твоему SSH ключу
      
  vars:
    project_dir: /home/{{ ansible_user }}/devops-todo-app
    docker_compose_version: "2.23.0"